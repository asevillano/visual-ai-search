# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: visual-ai-search
metadata:
  template: visual-ai-search

services:
  web:
    project: .
    language: py
    host: containerapp
    docker:
      path: Dockerfile
